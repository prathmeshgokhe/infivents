import features from "./features-schema";
import hero from "./hero-schema";
import mainFeaturesScheam from "./main-features-schema";

const schemas = [hero, features, mainFeaturesScheam];

export default schemas;
